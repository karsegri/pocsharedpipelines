steps:
  - task: Docker@2
    displayName: Build and push an image to container registry
    inputs:
      command: buildAndPush
      repository: 'pocsharedpipelines.v1'
      dockerfile: '**/Dockerfile'
      containerRegistry: 'pocsharedacr'
      tags: |
        $(tag)